<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.foodprojectgive">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FoodProjectGiveNoActionBar">
        <activity android:name=".SetUpActivity.WelcomeAnim">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SetUpActivity.FinalStepUpActivity"></activity>
        <activity android:name=".SetUpActivity.OtpActivity"></activity>
        <activity android:name=".SetUpActivity.ResetActivity" />
        <activity android:name=".SetUpActivity.SignUpActivity" />
        <activity android:name=".SetUpActivity.LogInActivity" />
        <activity android:name=".SetUpActivity.LogInSignUpActivity">

        </activity>
        <activity android:name=".SetUpActivity.MainActivity" />
    </application>

</manifest>